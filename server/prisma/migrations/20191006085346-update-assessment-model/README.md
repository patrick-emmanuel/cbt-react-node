# Migration `20191006085346-update-assessment-model`

This migration has been generated by Patrick at 10/6/2019, 8:53:46 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Assessment"("author" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,"description" TEXT NOT NULL DEFAULT ''  ,"id" TEXT NOT NULL   ,"published" BOOLEAN NOT NULL DEFAULT false  ,"title" TEXT NOT NULL DEFAULT ''  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,PRIMARY KEY ("id"))
;

INSERT INTO "new_Assessment" ("author","createdAt","description","id","published","title","updatedAt") SELECT "author","createdAt","description","id","published","title","updatedAt" from "Assessment"

DROP TABLE "lift"."Assessment";

ALTER TABLE "lift"."new_Assessment" RENAME TO "Assessment";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190928103446-change-test-model-to-assessment..20191006085346-update-assessment-model
--- datamodel.dml
+++ datamodel.dml
@@ -26,10 +26,10 @@
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   published Boolean
   title     String
-  content   String?
-  author    User?
+  description   String
+  author    User
   questions Question[]
 }
 model QuestionOption {
@@ -44,11 +44,17 @@
   id        String   @default(cuid()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   content   String
+  questionType QuestionType @default(TEXT)
   options   QuestionOption[]
 }
+enum QuestionType {
+  TEXT
+  SELECT
+}
+
 model User {
   id       String  @default(cuid()) @id
   email    String  @unique
   password String
```

## Photon Usage

You can use a specific Photon built for this migration (20191006085346-update-assessment-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191006085346-update-assessment-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
