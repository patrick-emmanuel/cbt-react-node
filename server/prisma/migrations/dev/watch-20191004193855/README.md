# Migration `watch-20191004193855`

This migration has been generated by Patrick at 10/4/2019, 7:38:55 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Assessment"("author" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,"createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,"description" TEXT NOT NULL DEFAULT ''  ,"id" TEXT NOT NULL   ,"published" BOOLEAN NOT NULL DEFAULT false  ,"title" TEXT NOT NULL DEFAULT ''  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,PRIMARY KEY ("id"))
;

INSERT INTO "new_Assessment" ("author","createdAt","description","id","published","title","updatedAt") SELECT "author","createdAt","description","id","published","title","updatedAt" from "Assessment"

DROP TABLE "lift"."Assessment";

ALTER TABLE "lift"."new_Assessment" RENAME TO "Assessment";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20190928203145..watch-20191004193855
--- datamodel.dml
+++ datamodel.dml
@@ -26,10 +26,10 @@
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   published Boolean
   title     String
-  description   String?
-  author    User?
+  description   String
+  author    User
   questions Question[]
 }
 model QuestionOption {
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191004193855)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191004193855'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
